package com.example.calculator;
public enum EngineState{ //enum used to keep track of the calculator's state for the processing inputs.
    INITIALIZED,READING_OPERAND_A,READING_OPERAND_B,DISPLAYING_RESULT
}
